delete:
  tags: [User-Command]
  summary: 사용자 회원 탈퇴
  operationId: withdraw
  security:
    - BearerAuth: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/common/BaseResponse.yaml'