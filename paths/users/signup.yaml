post:
  tags: [User-Command]
  summary: 사용자 회원가입
  operationId: signup
  security:
    - UserTokenAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/user/UserSignupRequest.yaml'
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/common/BaseResponse.yaml'