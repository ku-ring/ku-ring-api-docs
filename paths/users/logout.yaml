post:
  tags: [User-Command]
  summary: 사용자 로그아웃
  operationId: logout
  security:
    - UserTokenAuth: []
    - BearerAuth: []
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/common/BaseResponse.yaml'