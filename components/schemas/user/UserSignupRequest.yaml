type: object
properties:
  email:
    type: string
    format: email
    description: 건국대 이메일 (아이디로 사용)
    example: "newuser@konkuk.ac.kr"
  password:
    type: string
    description: 비밀번호 (8자 이상)
    example: "strongPassword123!"
required:
  - email
  - password